@{
  Layout = "_Layout";
}

@using BestRestaurant.Models;
@model List<BestRestaurant.Models.Review>;

<h1>Restaurants</h1>

@if (@Model.Count == 0)
{
  <h3>No Reviews have been added yet!</h3>
} 

@foreach (Review review in Model)
{
  <li>@Html.ActionLink($"{review.Description}", "Details", new { id = review.ReviewId }) | @review.Restaurant.Name</li>
}

<p>@Html.ActionLink("Add new review", "Create")</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p>